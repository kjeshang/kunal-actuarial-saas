import { InterestRateFormat } from './interest-rate-format';

describe('InterestRateFormat', () => {
  it('should create an instance', () => {
    const directive = new InterestRateFormat();
    expect(directive).toBeTruthy();
  });
});
