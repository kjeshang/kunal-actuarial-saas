import { CurrencyFormat } from './currency-format';

describe('CurrencyFormat', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormat();
    expect(directive).toBeTruthy();
  });
});
